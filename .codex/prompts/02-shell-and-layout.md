# Codex Phase 02 · App Shell and Layout

Implement Tasks 1–4 of the implementation plan.

Required outcome:

- App state/reducer boundary
- Vitest/Testing Library
- resizable left/center/right shell
- right overlay below 1100px
- left/right overlay below 900px
- focused Top Command Bar
- existing Sidebar, Editor and AI still function
- PR #1 `flushUnsaved()` behavior remains

Use existing `react-resizable-panels` and Radix. Do not introduce a new UI framework.

Capture 1440×900, 1200×800, 1024×768 and 800×600 evidence. Commit each task separately and do not continue while tests are failing.
