# Codex Phase 01 · PR #1 Baseline

Read `AGENTS.md`, `docs/memoji-ga/01_CURRENT_UI_AUDIT.md` and Task 0 of the implementation plan.

1. Verify current branch and uncommitted changes.
2. Fetch PR #1 into `memoji-pr1-baseline`.
3. Confirm the branch contains `flushUnsaved`, serialized save handling, two-pane Settings, managed local runtime and stream batching.
4. Create `codex/memoji-2-ga-uiux` from that baseline.
5. Run `npm run check`, Rust fmt, clippy and tests.
6. Write `docs/implementation/baseline-report.md`.
7. Do not change product behavior in this phase.
8. Commit only the baseline report and fixture policy.

Finish with exact test results and any baseline defects.
